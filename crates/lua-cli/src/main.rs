//! Standalone `lua` interpreter (the CLI). Wraps lua-vm + lua-stdlib.

fn main() {
    eprintln!("lua-rs: not yet implemented (pre-Phase-A)");
    std::process::exit(1);
}

// ──────────────────────────────────────────────────────────────────────────
// PORT STATUS
//   source:        (skeleton; Phase F populates from lua.c)
//   target_crate:  lua-cli
//   confidence:    high
//   todos:         0
//   port_notes:    0
//   unsafe_blocks: 0
//   notes:         placeholder main; CLI lands after vm + stdlib are stable
// ──────────────────────────────────────────────────────────────────────────
