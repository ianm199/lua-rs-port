You are a Debug agent for a Lua 5.4 to Rust port. A test program failed
with a REAL bug (not a todo stub). Find the root cause and fix it.

Test mode: __MODE__   (one of: inline, file)
Test program: __PROG__
Failure output (tail):
__OUTPUT__

If mode is "inline": __PROG__ is the literal Lua source string. Reproduce
with: target/debug/lua-rs "$prog"
If mode is "file": __PROG__ is a path to a .lua file under
reference/lua-c/testes/. Reproduce with:
  ./harness/run_official_test.sh __PROG__
First Read the file at __PROG__ to understand what construct it tests.

This means a recently-implemented function returns the wrong value
somewhere, or a compile-time codegen invariant is wrong. Find the
root cause. The fix must be real — do NOT paper over the error.

Process:

1. Read the panic / error context above carefully. Note any file:line
   coordinates.

2. Trace the call path. Entry points:
   - crates/lua-cli/src/main.rs (the cli driver)
   - crates/lua-stdlib/src/init.rs (open_libs)
   - crates/lua-stdlib/src/auxlib.rs (load_string + pcall_k)
   - crates/lua-parse/src/lib.rs (parser + codegen)
   - crates/lua-vm/src/vm.rs (bytecode dispatch)
   - crates/lua-vm/src/state.rs (LuaState + Phase-B stub impls)

3. Add temporary eprintln instrumentation if needed to localize which
   call returns the unexpected value. After each batch:
     cargo build -q -p lua-cli
     # mode=inline:
     target/debug/lua-rs "__PROG__" 2>&1 | tail -20
     # mode=file:
     ./harness/run_official_test.sh __PROG__ 2>&1 | tail -20
   (Build first, then exec the binary or wrapper directly — do not use
   cargo run inside the loop because it may hang. Cap your investigation
   to 5 such invocations.)
   Iterate until you isolate the buggy function.

4. Identify the root cause. Likely categories:
   - A codegen invariant fails (assertion in lua-parse)
   - A VM dispatch arm has wrong arity / register offset
   - A method returns nil / wrong type
   - A Phase-B stub method returns a placeholder

5. Fix the root cause. Acceptable fixes:
   - Implement a missing Phase-B inherent method on LuaState
   - Correct a wrong index/offset in codegen
   - Wire up a TODO(port) that was left as a stub
   - Translate a sibling C function that was incompletely ported

6. REMOVE all temporary eprintln instrumentation before stopping.

7. Verify with one final cargo build -p lua-cli that compile is green.
   Do NOT run the program at the end — the mega-loop will re-test.

Constraints:
- Edit files under crates/. You MAY touch lua-types if (and only if)
  the bug is genuinely there and the type-vocabulary hook permits it.
- Respect PreToolUse type-vocab hook.
- Do NOT replace todos bodies that aren't relevant to this bug.
- Do NOT use std::process, tokio, async, futures, rayon.

Report (under 200 words):
- The root cause (which function/file/line returns the unexpected value)
- The fix (what you changed, in 1-2 sentences)
- The new state after your fix: does the test program produce different
  output now? (If you didn't fully re-test, say so.)
